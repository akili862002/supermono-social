import styled, { css } from "styled-components";

export const TitleInputContainer = styled.span``;

export const Input = styled.input`
  background: none;
  border: none;
  font-size: inherit;
  color: inherit;
  font-weight: inherit;
  width: 100%;
  cursor: text;
`;
